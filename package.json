{
  "name": "lineimation",
  "version": "0.2.17-recovery",
  "private": true,
  "description": "Dependency-free reconstruction of a recovered curve-addressed 2D texture animation renderer",
  "scripts": {
    "check": "node --check src/curves.js && node --check src/core.js && node --check src/app.js && node scripts/validate.js",
    "test": "npm run check && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
